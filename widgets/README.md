WortKrieg widget examples live here.

- `ios/WortKriegWidget.swift`: WidgetKit small/medium/large example fed by a shared JSON snapshot.
- `android/WortKriegWidget.kt`: Glance widget example for Android.

These files are basic starter implementations intended to be moved into native targets when the app adds `ios/` and `android/` folders.
